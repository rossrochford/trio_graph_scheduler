from setuptools import setup

# to install run: python setup.py install

setup(
    name="trio_graph_scheduler",
    version="0.0.01",
    author="Ross Rochford",
    packages=['trio_graph_scheduler'],   
    dependeny_links=[],
    classifiers=[],
)
